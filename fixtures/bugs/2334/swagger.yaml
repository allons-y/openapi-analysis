openapi: '3.0.0'
info:
  title: Object
  version: 0.1.0
paths: 
   /bar:
     get:
      responses:
        200:
          description: OK
          schema:
            $ref: "./bar.yaml#/components/schemas/Bar"
