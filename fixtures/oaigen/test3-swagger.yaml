openapi: '3.0.0'
info:
  version: 0.1.1
  title: test 1
  description: recursively following JSON references
  contact:
    name: Fred

schemes:
  - http

consumes:
  - application/json
produces:
  - application/json

paths:
  /getAll:
    get:
      operationId: getAll
      parameters:
        - name: a
          in: body
          description: max number of results
          required: false
          schema:
            $ref: '#/components/schemas/a'
      responses:
        '200':
          description: Success
          schema:
            $ref: '#/components/schemas/b'
        '203':
          description: Success
          schema:
            $ref: '#/components/schemas/c'

definitions:
  a:
    type: string
  b:
    $ref: 'test3-model-schema.json#/components/schemas/b'
  c:
    $ref: 'test3-model-schema.json#/components/schemas/c'


